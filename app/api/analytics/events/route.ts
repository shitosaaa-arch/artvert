import { NextResponse } from "next/server"; import { recordAnalyticsEvent } from "@/lib/analytics/service"; import { enforceRateLimit } from "@/lib/customers/rate-limit"; import { requestIpHash } from "@/lib/customers/security";
export async function POST(request: Request) { try { enforceRateLimit("analytics", requestIpHash(request), 120, 60 * 1000); await recordAnalyticsEvent(await request.json()); return new NextResponse(null, { status: 204 }); } catch (error) { const code = error instanceof Error ? error.message : "INVALID_ANALYTICS_EVENT"; return NextResponse.json({ error: code }, { status: code === "RATE_LIMITED" ? 429 : 400 }); } }
