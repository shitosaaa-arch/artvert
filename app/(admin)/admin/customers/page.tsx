import { requireCustomerAdmin } from "@/lib/customers/staff"; import { getPrismaClient } from "@/lib/db/prisma";
export const dynamic = "force-dynamic";
export default async function AdminCustomersPage() { await requireCustomerAdmin(); const customers = await getPrismaClient().customer.findMany({ select: { id: true, email: true, displayName: true, active: true, emailVerifiedAt: true, createdAt: true }, orderBy: { createdAt: "desc" }, take: 50 }); return <main className="p-8" dir="rtl"><h1 className="text-3xl font-black">إدارة العملاء</h1><p className="my-3 text-slate-300">هذه القائمة لا تعرض سجل الطبيب أو الصور أو البيانات الخاصة.</p><div className="overflow-x-auto rounded bg-white text-slate-900"><table className="w-full text-right"><thead><tr><th className="p-3">الاسم</th><th>البريد</th><th>الحالة</th><th>تاريخ الإنشاء</th></tr></thead><tbody>{customers.map((customer) => <tr key={customer.id} className="border-t"><td className="p-3">{customer.displayName}</td><td>{customer.email}</td><td>{customer.active ? "نشط" : "موقوف"}</td><td>{customer.createdAt.toLocaleDateString("ar-EG")}</td></tr>)}</tbody></table></div></main>; }
