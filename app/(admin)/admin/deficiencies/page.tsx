import type { Metadata } from "next"; import AdminSidebar from "@/components/admin/AdminSidebar"; import DeficiencyManager from "@/components/admin/DeficiencyManager"; import { UserRole } from "@/lib/auth/roles"; import { requireRole } from "@/lib/auth/session"; export const metadata: Metadata = { title: "Nutrient deficiency management" }; export default async function DeficienciesPage() { await requireRole(UserRole.SUPER_ADMIN, UserRole.ADMIN, UserRole.AGRONOMIST); return <div className="lg:flex"><AdminSidebar /><DeficiencyManager /></div>; }
