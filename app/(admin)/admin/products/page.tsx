import type { Metadata } from "next"; import AdminSidebar from "@/components/admin/AdminSidebar"; import ProductManager from "@/components/admin/ProductManager"; import { requireRole } from "@/lib/auth/session"; import { UserRole } from "@/lib/auth/roles"; export const metadata:Metadata={title:"Product management"}; export default async function ProductsAdmin(){await requireRole(UserRole.SUPER_ADMIN,UserRole.ADMIN,UserRole.AGRONOMIST);return <div className="lg:flex"><AdminSidebar/><ProductManager/></div>}
