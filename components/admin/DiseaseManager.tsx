"use client";
import { FormEvent, useCallback, useEffect, useState } from "react";
type Disease = { id: string; classification: string; severity: string; scientificName: string | null; entity: { name: string; slug: string }; symptoms: { value: string }[]; syncState: { status: string } | null };
const blank = { name: "", slug: "", classification: "FUNGAL", severity: "MODERATE", scientificName: "", aliases: "", symptoms: "" };
export default function DiseaseManager() {
  const [items, setItems] = useState<Disease[]>([]), [page, setPage] = useState(1), [pages, setPages] = useState(1), [q, setQ] = useState(""), [type, setType] = useState(""), [severity, setSeverity] = useState(""), [sync, setSync] = useState(""), [sort, setSort] = useState("updatedAt"), [form, setForm] = useState(blank), [selected, setSelected] = useState<Disease | null>(null), [editing, setEditing] = useState(false), [message, setMessage] = useState("");
  const load = useCallback(async () => { const p = new URLSearchParams({ page: String(page), pageSize: "12", sort, direction: "desc" }); for (const [key, value] of [["q", q], ["classification", type], ["severity", severity], ["syncStatus", sync]]) if (value) p.set(key, value); const response = await fetch(`/api/admin/diseases?${p}`), data = await response.json(); if (response.ok) { setItems(data.items); setPages(data.pageCount); } else setMessage(data.error); }, [page, q, type, severity, sync, sort]);
  useEffect(() => { const timer = setTimeout(() => { void load(); }, 0); return () => clearTimeout(timer); }, [load]);
  const submit = async (event: FormEvent) => { event.preventDefault(); const payload = { ...form, aliases: form.aliases.split(",").map((value) => value.trim()).filter(Boolean), symptoms: form.symptoms.split(",").map((value) => value.trim()).filter(Boolean) }; const response = await fetch(selected ? `/api/admin/diseases/${selected.id}` : "/api/admin/diseases", { method: selected ? "PATCH" : "POST", headers: { "content-type": "application/json" }, body: JSON.stringify(payload) }), data = await response.json(); if (!response.ok) return setMessage(data.error); setEditing(false); setSelected(data); await load(); };
  const retry = async (disease: Disease) => { const response = await fetch(`/api/admin/diseases/${disease.id}/sync`, { method: "POST" }); setMessage(response.ok ? "Knowledge sync completed." : "Knowledge sync failed."); await load(); };
  return <main className="min-h-screen flex-1 bg-[#07140f] p-5 text-white lg:p-10"><div className="mx-auto max-w-6xl"><header className="mb-6 flex justify-between"><h1 className="text-3xl font-black">Disease management</h1><button onClick={() => { setForm(blank); setSelected(null); setEditing(true); }} className="rounded bg-emerald-300 px-4 py-2 font-bold text-black">New disease</button></header><div className="mb-4 grid gap-2 md:grid-cols-5"><input value={q} onChange={(event) => { setQ(event.target.value); setPage(1); }} placeholder="Search name, symptom, alias" /><select value={type} onChange={(event) => setType(event.target.value)}><option value="">All classifications</option>{["FUNGAL", "BACTERIAL", "VIRAL", "OOMYCETE", "PHYSIOLOGICAL_DISORDER"].map((value) => <option key={value}>{value}</option>)}</select><select value={severity} onChange={(event) => setSeverity(event.target.value)}><option value="">All severity</option>{["LOW", "MODERATE", "HIGH", "CRITICAL"].map((value) => <option key={value}>{value}</option>)}</select><select value={sync} onChange={(event) => setSync(event.target.value)}><option value="">All sync states</option>{["PENDING", "SYNCED", "FAILED"].map((value) => <option key={value}>{value}</option>)}</select><select value={sort} onChange={(event) => setSort(event.target.value)}><option value="updatedAt">Recently updated</option><option value="createdAt">Recently created</option><option value="name">Name</option></select></div>{message && <p>{message}</p>}<div className="rounded border border-white/10">{items.map((disease) => <div key={disease.id} className="flex justify-between border-b border-white/10 p-4"><button onClick={() => { setSelected(disease); setEditing(false); }}><b>{disease.entity.name}</b> · {disease.classification} · {disease.severity} · {disease.syncState?.status ?? "PENDING"}</button><button onClick={() => void retry(disease)}>Retry sync</button></div>)}</div><div className="mt-3 flex gap-3"><button disabled={page === 1} onClick={() => setPage(page - 1)}>Previous</button><span>{page}/{pages}</span><button disabled={page === pages} onClick={() => setPage(page + 1)}>Next</button></div>{(editing || selected) && <div className="fixed inset-0 bg-black/75 p-5"><div className="mx-auto max-w-xl bg-[#0b2118] p-6">{editing ? <form onSubmit={submit} className="grid gap-3">{(["name", "slug", "scientificName", "aliases", "symptoms"] as const).map((key) => <input key={key} required={key === "name"} value={form[key]} placeholder={key} onChange={(event) => setForm({ ...form, [key]: event.target.value })} />)}<select value={form.classification} onChange={(event) => setForm({ ...form, classification: event.target.value })}>{["FUNGAL", "BACTERIAL", "VIRAL", "OOMYCETE", "PHYSIOLOGICAL_DISORDER"].map((value) => <option key={value}>{value}</option>)}</select><select value={form.severity} onChange={(event) => setForm({ ...form, severity: event.target.value })}>{["LOW", "MODERATE", "HIGH", "CRITICAL"].map((value) => <option key={value}>{value}</option>)}</select><button>Save disease</button></form> : selected && <><h2>{selected.entity.name}</h2><p>{selected.scientificName}</p><p>Symptoms: {selected.symptoms.map((symptom) => symptom.value).join(", ")}</p></>}<button onClick={() => { setSelected(null); setEditing(false); }}>Close</button></div></div>}</div></main>;
}
