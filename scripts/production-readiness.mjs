import assert from "node:assert/strict"; import { readFile } from "node:fs/promises"; import path from "node:path";
const read = (file) => readFile(path.join(process.cwd(), file), "utf8");
const main = async () => { const [config, robots, sitemap, workflow, migration] = await Promise.all([read("next.config.ts"), read("app/(public)/robots.ts"), read("app/(public)/sitemap.ts"), read(".github/workflows/prisma-linux.yml"), read("prisma/migrations/20260802070000_add_customer_accounts_analytics/migration.sql")]); for (const header of ["Content-Security-Policy", "Strict-Transport-Security", "X-Frame-Options", "Referrer-Policy", "Permissions-Policy"]) assert.ok(config.includes(header), `${header} missing`); assert.ok(robots.includes("sitemap") && sitemap.includes("/products"), "SEO routes missing"); for (const command of ["db:validate", "db:generate", "db:deploy", "db:status", "test:customers:integration", "test:doctor", "test:vision", "npm run build", "npm run lint", "tsc --noEmit", "git diff --check"]) assert.ok(workflow.includes(command), `${command} missing from Linux verification`); assert.ok(migration.includes("Customer") && migration.includes("Analytics"), "customer migration missing"); console.log("production-readiness-tests:ok"); };
main().catch((error) => { console.error(error.message); process.exitCode = 1; });
