import dotenv from "dotenv"; import { DeficiencyCleanupProcessor } from "../lib/deficiencies/deficiency-cleanup"; dotenv.config({ path: ".env.local" }); async function main() { const processor = new DeficiencyCleanupProcessor(); console.log(`deficiency-cleanup: queued ${await processor.queueOrphans()} orphan(s), processed ${await processor.processPending(100)} job(s)`); } main().catch((error) => { console.error(error); process.exitCode = 1; });
