import assert from "node:assert/strict"; import { promises as fs } from "node:fs"; import path from "node:path";
async function main(){const root=process.cwd();const [schema,pipeline,template]=await Promise.all([fs.readFile(path.join(root,"prisma/schema.prisma"),"utf8"),fs.readFile(path.join(root,"lib/knowledge/import-pipeline.ts"),"utf8"),fs.readFile(path.join(root,"data/knowledge-import/templates/plants.json"),"utf8")]);for(const model of ["KnowledgeSource","KnowledgeImageRights","KnowledgeImportBatch","KnowledgeProvenance"])assert.ok(schema.includes(`model ${model}`));for(const rule of ["approved commercial source required","approved image rights required","treatment authority required","BATCH_NOT_APPROVED"])assert.ok(pipeline.includes(rule));assert.equal(JSON.parse(template)[0].reviewerStatus,"APPROVED");console.log("knowledge-import-tests:ok");}main().catch(e=>{console.error(e);process.exitCode=1;});
